import * as accounts from '@aztec/accounts/schnorr';
import * as accountsMain from '@aztec/accounts';
console.log('schnorr exports:', Object.keys(accounts).join('\n'));
console.log('\naccounts main exports:', Object.keys(accountsMain).join('\n'));
