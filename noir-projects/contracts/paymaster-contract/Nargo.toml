[package]
name = "native_paymaster"
type = "contract"
version = "0.1.0"
authors = ["Ledger Network / Studio Provenance"]

# Uses aztec-nr — the official Noir library for Aztec contracts.
# Tag matches the installed aztec CLI version (4.4.0).
[dependencies]
aztec = { git = "https://github.com/AztecProtocol/aztec-nr", tag = "v4.4.0", directory = "aztec" }
